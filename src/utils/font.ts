import { Work_Sans, Open_Sans } from 'next/font/google';

export const work = Work_Sans({ subsets: ['latin'], display: 'swap' });

export const open = Open_Sans({ subsets: ['latin'], display: 'swap' });
