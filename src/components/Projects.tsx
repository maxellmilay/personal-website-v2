import React from 'react';
import { work } from '@/utils/font';

function Projects() {
  return (
    <div className="flex w-[55rem] mb-16 justify-center">
      <p className={`${work.className} text-2xl`}>Other Projects</p>
    </div>
  );
}

export default Projects;
